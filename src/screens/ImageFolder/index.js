import React from 'react';
import { View, Text } from 'react-native';

const ImageFolder = () => {
	return (
		<View style={ { flex: 1, alignItems: 'center', justifyContent: 'center' } }>
			<Text>Image Folder Screen</Text>
		</View>
	);
};

export default ImageFolder;
