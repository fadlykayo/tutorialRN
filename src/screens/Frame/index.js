import React from 'react';
import { View, Text } from 'react-native';

const Frame = () => {
	return (
		<View style={ { flex: 1, alignItems: 'center', justifyContent: 'center' } }>
			<Text>Frame Screen</Text>
		</View>
	);
};

export default Frame;
